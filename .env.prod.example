SECRET_KEY=REPLACE_WITH_python3_-c_import_secrets_print_secrets_token_urlsafe_60

DEBUG=False
USE_HTTPS=True

# Multi-app VPS: Docker nginx publishes HTTP_PORT (8088); host nginx proxies dcpmind.com → 127.0.0.1:8088
ALLOWED_HOSTS=dcpmind.com,www.dcpmind.com,159.195.52.197,127.0.0.1,localhost,nginx,web
CSRF_TRUSTED_ORIGINS=https://dcpmind.com,https://www.dcpmind.com

HTTP_PORT=8088

POSTGRES_DB=ntarque
POSTGRES_USER=ntarque
POSTGRES_PASSWORD=REPLACE_STRONG_PASSWORD_ALNUM_ONLY

TIME_ZONE=UTC

GUNICORN_WORKERS=2

# Optional — only if you set a custom Fernet key for encrypted task titles
TASK_ENCRYPTION_KEY=
