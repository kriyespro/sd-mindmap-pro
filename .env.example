# Copy to .env and set values before production deploy.

# Required — generate with: python -c "import secrets; print(secrets.token_urlsafe(60))"
SECRET_KEY=your-long-random-secret-key-here

# Set to False in production
DEBUG=False

# False for HTTP-only (droplet IP). True when you serve HTTPS or terminate TLS at a proxy.
USE_HTTPS=False

# Comma-separated hostnames/IPs (include Docker service names if needed: web,nginx)
ALLOWED_HOSTS=localhost,127.0.0.1,nginx,web,yourdomain.com

# HTTPS origins for CSRF (Django 4+), comma-separated, include scheme and port if not 80/443
# Example: https://app.example.com,https://www.example.com
# HTTP on custom port: http://YOUR_IP:8080
CSRF_TRUSTED_ORIGINS=

# Optional — host port mapped to nginx:80 in docker-compose (default 80). Uncomment for e.g. http://IP:8080/
# HTTP_PORT=8080

# PostgreSQL (docker-compose defaults match POSTGRES_* below)
POSTGRES_DB=ntarque
POSTGRES_USER=ntarque
POSTGRES_PASSWORD=change-me-in-production

# Docker Compose sets POSTGRES_HOST=db and passes POSTGRES_* into the web container (see docker-compose.yml).
# For hosted Postgres without Docker, use DATABASE_URL instead (postgres://...).

# Optional: Asia/Kolkata, UTC, America/New_York, etc.
TIME_ZONE=UTC

# Gunicorn workers (default 1 — raise on larger VPS if you have RAM)
# GUNICORN_WORKERS=2
